# syntax=docker/dockerfile:1
# Dev-only image — hot-reload via docker-compose bind mount.
# Production uses the multi-stage build in Dockerfile instead.
FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host"]
